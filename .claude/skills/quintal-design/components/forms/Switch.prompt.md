A preference toggle.

```jsx
<Switch checked={morning} onChange={setMorning} label="Receber sugestões pela manhã" />
```

Off is a neutral grey, not an alarm colour — turning something off is never a failure.
