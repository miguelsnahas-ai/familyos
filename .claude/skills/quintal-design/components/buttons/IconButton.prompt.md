A 44px circular button for a single line icon — menu, close, back.

```jsx
<IconButton label="Fechar"><Icon name="x" /></IconButton>
```

Never use it for a primary action; the family should always read a verb.
