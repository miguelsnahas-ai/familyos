The only icon source in Quintal. No emoji, ever.

```jsx
<Icon name="moon" /> <Icon name="arrowRight" size={16} />
```

`Icon.names` lists the set. Pillar mapping: sono → moon, rotina → clock, livre brincar → sparkle, desenvolvimento → blocks.
