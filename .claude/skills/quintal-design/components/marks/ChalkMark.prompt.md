Chalk marginalia — the brand's one decorative device. Marks only: underline, star, arrow, sun. Never draw characters or interface icons in chalk.

```jsx
<ChalkDefs />
<ChalkMark mark="star" color="var(--lilas)" />
```

Use on plum blocks, at most one cluster per screen.
